import 'package:flutter/material.dart';

class RootNavigatorPage01 extends StatelessWidget {
  const RootNavigatorPage01({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Root Navigator Page 01'),
      ),
    );
  }
}